% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{autoplot.balancing}
\alias{autoplot.balancing}
\alias{plot.balancing}
\title{Diagnostic plots for a balancing fit}
\arguments{
\item{object, x}{A \link{balancing} fit.}

\item{type}{The view to draw, one of \code{"balance"}, \code{"weights"}, or \code{"duals"}.}

\item{y}{Not used; present for compatibility with the \code{\link[=plot]{plot()}} generic.}

\item{...}{Passed on. \code{plot()} forwards these arguments, including \code{type}, to
\code{autoplot()}; \code{autoplot()} itself accepts no further arguments.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object. \code{plot()} returns it invisibly after
printing.
}
\description{
\code{autoplot()} draws one of three diagnostic views of a \link{balancing} fit, and
\code{plot()} prints that view. These plots diagnose the optimization: \code{"balance"}
is a love plot contrasting the unweighted and weighted absolute standardized
mean differences (exposure-covariate correlations for a continuous exposure)
against a dashed tolerance line; \code{"weights"} shows the distribution of the
balancing weights by exposure group; and \code{"duals"} shows the solver's dual
variables as a bar chart, so the constraints that bind the quadratic program
stand out.
}
\details{
The \code{"duals"} view needs the solver dual variables the quadratic-program
family reports. The estimating-equation family (entropy balancing, inverse
probability tilting, the covariate balancing propensity score) carries none,
so \code{"duals"} raises an error for those fits.

These views target the balancing method itself. To assess covariate balance
more broadly, extract the weights with \code{\link[=weights]{weights()}} and pass them to the
halfmoon package, whose love plots and distribution summaries cover a general
balance-assessment workflow.
}
\examples{
n <- 200
x1 <- rnorm(n)
x2 <- rnorm(n)
df <- data.frame(
  exposure = rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2)),
  x1 = x1,
  x2 = x2
)
fit <- balance(df, exposure, c(x1, x2), method = bal_entropy())

# Requires ggplot2.
if (rlang::is_installed("ggplot2")) {
  ggplot2::autoplot(fit, type = "balance")
  ggplot2::autoplot(fit, type = "weights")
}

}
