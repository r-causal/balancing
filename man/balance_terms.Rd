% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{balance_terms}
\alias{balance_terms}
\title{Balance constraints}
\usage{
balance_terms(
  moments = NULL,
  interactions = FALSE,
  quantiles = NULL,
  tolerance = 0,
  ...
)
}
\arguments{
\item{moments}{The highest covariate power to balance. A single whole number
or a named integer vector; \code{NULL} (the default) resolves to first moments.}

\item{interactions}{Whether to add pairwise interactions of the base columns.}

\item{quantiles}{Quantile probabilities in \verb{(0, 1)}: a numeric vector applied
to every continuous covariate, a named list of probabilities per covariate,
or \code{NULL} for none.}

\item{tolerance}{The per-constraint tolerance: a single non-negative number,
or a named vector giving the tolerance per source covariate.}

\item{...}{Reserved for future extensions; must be empty.}
}
\value{
A \code{balance_terms} object.
}
\description{
\code{balance_terms()} records the set of covariate functions a balancing method
should equate across exposure groups. It is passed to \code{\link[=balance]{balance()}} through
\code{constraints}. The specification is data-free: the covariate expansion it
describes is applied at fit time.
}
\details{
The constraint set is built from four ingredients:
\itemize{
\item \code{moments}: the highest power of each numeric covariate to balance. A scalar
applies to every covariate; a named integer vector sets powers per
covariate, with unnamed covariates defaulting to \code{1}. Powers above \code{1} are
ignored for binary indicator columns.
\item \code{interactions}: when \code{TRUE}, all pairwise products of distinct base columns
are added, excluding products of two indicators of the same factor.
\item \code{quantiles}: probabilities in \verb{(0, 1)}. Each probability adds an indicator
column so that mean balance on the indicator is quantile balance on the
covariate. A single probability vector applies to every continuous
covariate; a named list sets probabilities per covariate. Quantile
constraints apply to discrete exposures only.
\item \code{tolerance}: the largest absolute standardized mean difference (discrete
exposures) or exposure-covariate correlation (continuous exposures)
permitted per constraint. A scalar applies to every covariate; a named
vector sets tolerances per source covariate, and derived columns inherit
their source covariate's tolerance. \code{0} requests exact balance. A positive
value selects the inexact problem for entropy balancing and is the central
tuning parameter for stable balancing weights.
}
}
\examples{
# Balance means and variances of every numeric covariate.
balance_terms(moments = 2)

# Balance means with a relaxed tolerance.
balance_terms(tolerance = 0.05)

}
