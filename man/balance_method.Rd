% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{balance_method}
\alias{balance_method}
\alias{estimating_equation_method}
\alias{quadratic_program_method}
\title{The balancing method specification classes}
\usage{
balance_method(convergence_tolerance = NULL, max_iterations = NULL)

estimating_equation_method(convergence_tolerance = NULL, max_iterations = NULL)

quadratic_program_method(
  convergence_tolerance = NULL,
  max_iterations = NULL,
  weight_penalty = numeric(0),
  min_weight = numeric(0)
)
}
\arguments{
\item{convergence_tolerance}{The solver convergence tolerance, or \code{NULL} to
leave it to the solver. The value resolved for \code{NULL} differs by family:
\code{1e-10} on the gradient for the estimating-equation methods, and \code{1e-8} as
both the absolute and the relative tolerance for the quadratic-program
methods.}

\item{max_iterations}{The maximum solver iterations, or \code{NULL} to leave the
cap to the solver. The value resolved for \code{NULL} is 1000 for the
estimating-equation methods and 200000 for the quadratic-program methods.}

\item{weight_penalty}{The L2 penalty on the weights.}

\item{min_weight}{The smallest permitted weight.}
}
\value{
An abstract class object. Constructing a concrete subclass returns a
\code{balance_method}.
}
\description{
The method constructors (\code{\link[=bw_entropy]{bw_entropy()}} and its siblings) return small
S7 objects that carry tuning parameters and never touch data. They share an
abstract hierarchy so that \code{\link[=balance]{balance()}} can query a method's capabilities
uniformly. \code{balance_method} is the abstract root. The estimating-equation
family (entropy balancing, inverse probability tilting, the covariate
balancing propensity score) subclasses \code{estimating_equation_method}; the
quadratic-program family (energy, characteristic function distance, and
stable balancing weights) subclasses \code{quadratic_program_method}. None of the
abstract classes can be constructed directly.
}
