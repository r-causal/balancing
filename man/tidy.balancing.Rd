% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{tidy.balancing}
\alias{tidy.balancing}
\title{Tidy and glance methods for a balancing fit}
\arguments{
\item{x}{A \link{balancing} result.}

\item{...}{Ignored.}
}
\value{
A tibble, as described in the details.
}
\description{
\code{\link[generics:tidy]{generics::tidy()}} returns the fit's balance table, one row per constraint
term, and \code{\link[generics:glance]{generics::glance()}} returns a one-row fit summary. Both are
re-exported so they work with \code{library(balancing)} alone.
}
\details{
\code{tidy()} returns a tibble with one row per balance constraint and the
columns:
\itemize{
\item \code{term}: the covariate or expansion the constraint acts on.
\item \code{kind}: the constraint kind, such as \code{"moment"} or \code{"quantile"}.
\item \code{statistic}: the balance statistic, \code{"smd"} for a discrete exposure or
\code{"correlation"} for a continuous exposure.
\item \code{group}: the exposure level the contrast is measured against.
\item \code{unweighted}: the statistic before weighting.
\item \code{weighted}: the statistic after weighting.
\item \code{tolerance}: the requested tolerance for the term.
\item \code{within_tolerance}: whether the weighted statistic sits within tolerance.
}

\code{glance()} returns a one-row tibble with the columns:
\itemize{
\item \code{method}: the balancing method label.
\item \code{estimand}: the target estimand.
\item \code{exposure_type}: the resolved exposure type.
\item \code{n}: the number of observations.
\item \code{ess}: the overall effective sample size.
\item \code{n_constraints}: the number of balance constraints.
\item \code{max_absolute_smd} or \code{max_absolute_correlation}: the largest absolute
weighted statistic, named for the exposure type.
\item \code{converged}: whether the solver converged.
\item \code{iterations}: the iteration count the solver reported.
\item \code{objective}: the solver's objective value.
}
}
\examples{
n <- 200
x1 <- rnorm(n)
df <- data.frame(exposure = rbinom(n, 1, plogis(0.5 * x1)), x1 = x1)
fit <- balance(df, exposure, x1, method = bw_entropy())
tidy(fit)
glance(fit)

}
