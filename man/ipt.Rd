% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-ipt.R
\name{ipt}
\alias{ipt}
\title{Inverse probability tilting}
\usage{
ipt(
  link = c("logit", "probit", "cloglog"),
  convergence_tolerance = 1e-10,
  max_iterations = NULL,
  ...
)
}
\arguments{
\item{link}{The propensity link, one of \code{"logit"}, \code{"probit"}, or
\code{"cloglog"}.}

\item{convergence_tolerance}{The solver convergence tolerance on the tilting
moment.}

\item{max_iterations}{The maximum solver iterations, or \code{NULL} for the core
default.}

\item{...}{Reserved for future extensions; must be empty. Tuning parameters
must be passed by name.}
}
\value{
An \code{ipt} specification, a \link{balance_method}.
}
\description{
\code{ipt()} specifies inverse probability tilting for \code{\link[=balance]{balance()}}. A propensity
model is fit not by maximum likelihood but by a tilted moment condition that
forces each treatment group's weighted covariate means to their estimand
targets, so balance on the requested moments is exact by construction.
Inverse probability tilting supports binary and categorical exposures.
}
\details{
For each treatment level the propensity \verb{p_i = G(x_i' beta)} is estimated so
that the weighted covariate total matches the target population total. The
average treatment effect tilts every level to the whole sample, weighting a
unit by the inverse of its modeled propensity. A focal estimand tilts each
non-focal level to the focal level, weighting a unit by \code{(1 - p_i) / p_i},
and leaves the focal units at weight one. With mean balance and the logit
link the tilt solves the same treated-target problem as entropy balancing, so
the two methods produce the same average-treatment-effect-on-the-treated
weights for a binary exposure.

The weights solve smooth estimating equations regardless of the requested
tolerance, which \code{\link[=balance]{balance()}} records for later linearized inference.
}
\examples{
n <- 200
x1 <- rnorm(n)
x2 <- rnorm(n)
df <- data.frame(
  exposure = rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2)),
  x1 = x1,
  x2 = x2
)
fit <- balance(df, exposure, c(x1, x2), method = ipt())
fit

}
\references{
Graham, B. S., Pinto, C. C. de X., and Egel, D. (2012). Inverse probability
tilting for moment condition models with missing data. \emph{The Review of
Economic Studies}, 79(3), 1053-1079.
}
