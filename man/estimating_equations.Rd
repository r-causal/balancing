% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{estimating_equations}
\alias{estimating_equations}
\title{Extract the estimating-equations container}
\usage{
estimating_equations(x, ...)
}
\arguments{
\item{x}{A \link{balancing} result.}

\item{...}{Ignored.}
}
\value{
A \link{balancing_estimating_equations} object.
}
\description{
\code{estimating_equations()} returns the \link{balancing_estimating_equations} a fit
produced, the pieces a stacked sandwich variance needs after balancing. It is
available only for fits whose weights solve smooth estimating equations: the
estimating-equation family (entropy balancing, inverse probability tilting,
just-identified covariate balancing propensity score) with exact balance.
Fits without estimating equations, such as any tolerance-relaxed or
quadratic-program fit, raise \code{balancing_ipw_unsupported_error}.
}
\examples{
n <- 200
x1 <- rnorm(n)
df <- data.frame(exposure = rbinom(n, 1, plogis(0.5 * x1)), x1 = x1)
fit <- balance(df, exposure, x1, method = bal_entropy())
estimating_equations(fit)

}
