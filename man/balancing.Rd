% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{balancing}
\alias{balancing}
\title{A fitted balancing result}
\arguments{
\item{weights}{The balancing weights, a \link{bw} vector.}

\item{method}{The fitted \link{balance_method} specification.}

\item{estimand}{The resolved estimand string.}

\item{exposure}{The exposure column name.}

\item{exposure_type}{The resolved exposure type.}

\item{exposure_levels}{The exposure levels the fit weighted, as character
strings in the order \code{levels(factor(exposure))} gives: a factor's declared
order for a factor exposure, and the values sorted in their own type
otherwise, so a numeric dose of 9 and 10 orders 9 first. Levels no
observation takes are dropped and do not appear. The first element is the
reference level every contrast in \code{\link[=ipw.balancing]{ipw()}} is measured
against. A continuous exposure carries no levels, so the vector is empty.}

\item{covariates}{The covariate column names that contributed at least one
retained constraint column, in selection order. The constraint expansion
drops constant and aliased columns, and a covariate can be selected without
contributing any column at all, so this records what the fit constrained
rather than what was requested; the request itself stays in \code{call}. A fit
whose balance comes from its objective rather than from constraints, such as
energy or kernel balancing with no moment constraints, therefore records no
covariates even though its objective reads every selected one.}

\item{focal_level}{The focal exposure level for \code{"att"} and \code{"atc"}, or
\code{NULL}. This is the fitted object's property, set from the \code{.focal_level}
argument of \code{\link[=balance]{balance()}}.}

\item{n}{The number of observations.}

\item{constraints}{The resolved \link{balance_terms} specification, or \code{NULL}.}

\item{recipe}{The covariate expansion recipe, a list of per-column records.}

\item{balance_table}{The achieved balance, one row per constraint term.}

\item{duals}{Solver dual variables for diagnostics, or \code{NULL}.}

\item{coefficients}{The fitted coefficients or dual variables, or \code{NULL}.}

\item{converged}{Whether the solver met its convergence criterion.}

\item{iterations}{The solver iteration count. An energy fit that could not
reach its tolerance re-solves at a reachable one, and when that re-solve
converges this sums the original and the fallback solve, so it can exceed
the requested \code{max_iterations}. When the re-solve does not converge the
fit reports the original solve alone, so the count stays within the cap.
A continuous energy or stable balancing fit with a positive tolerance
refines the bound it hands the solver over several passes, each a solve of
its own, and this sums every one of them. See \code{\link[=bw_energy]{bw_energy()}} and \code{\link[=bw_sbw]{bw_sbw()}}
for the fuller account.}

\item{objective}{The solved objective value.}

\item{solver_status}{The solver that produced the result.}

\item{estimating_equations}{The \link{balancing_estimating_equations} container,
or \code{NULL}.}

\item{vcov}{The covariance of the fit's own weight parameters, a \code{p} by \code{p}
matrix named for them, or \code{NULL}. A covariance for those parameters comes
from the stacked system an \code{\link[=ipw.balancing]{ipw()}} result assembles, so
\code{\link[=balance]{balance()}} leaves this empty and the result fills it in on the copy of the
fit it stores, where \code{\link[stats:vcov]{stats::vcov()}} reads it back.}

\item{sampling_weights}{The sampling weights, or \code{NULL}.}

\item{call}{The originating call.}
}
\value{
A \code{balancing} object.
}
\description{
\code{balancing} is the S7 object \code{\link[=balance]{balance()}} returns. It carries the balancing
weights, the resolved method and estimand, the covariate expansion recipe, a
balance table, and the solver diagnostics. The raw data are not stored; the
recipe carries what is needed to rebuild the constraint matrix.
}
