% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balancing-package.R
\name{balancing_options}
\alias{balancing_options}
\title{Package options}
\description{
balancing reads a handful of global options, set with \code{\link[=options]{options()}}, that tune
how a fit runs without changing any function's signature.
}
\details{
\itemize{
\item \code{balancing.quiet}: when \code{TRUE}, suppresses the informational alerts
\code{\link[=balance]{balance()}} prints, such as the detected exposure type. Defaults to
\code{FALSE}.
\item \code{balancing.threads}: the number of worker threads the Rust core may use.
When unset, the count is resolved automatically from the physical core
count, capped by \code{OMP_THREAD_LIMIT} and \code{OMP_NUM_THREADS}, and forced to two
under \verb{R CMD check}.
\item \code{balancing.entropy_solver}: the solver for the exact entropy problem, one
of \code{"newton"} (the default), \code{"lbfgs"}, or \code{"lbfgs_then_newton"}. Newton is
the only solver that drives the estimating equations to machine precision;
the alternatives trade some precision for speed on large problems.
\item \code{balancing.qp_backend}: the quadratic-program backend for the energy, CFD,
and stable-balancing-weights methods, one of \code{"auto"} (the default),
\code{"osqp"}, or \code{"clarabel"}. Under \code{"auto"}, the default solver runs first and
the fit re-solves with the interior-point backend on a primal-infeasibility
certificate; \code{"osqp"} disables that fallback and \code{"clarabel"} uses the
interior-point backend directly.
}

Results are deterministic across thread counts: the same inputs produce
identical weights at any \code{balancing.threads} value.
}
\examples{
# Silence the exposure-type alert for a single fit.
withr::with_options(list(balancing.quiet = TRUE), {
  n <- 100
  x1 <- rnorm(n)
  df <- data.frame(exposure = rbinom(n, 1, plogis(x1)), x1 = x1)
  balance(df, exposure, x1, method = bw_entropy())
})
}
