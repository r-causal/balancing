% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-entropy.R
\name{bal_entropy}
\alias{bal_entropy}
\title{Entropy balancing}
\usage{
bal_entropy(
  ...,
  base_weights = NULL,
  distribution_moments = NULL,
  convergence_tolerance = 1e-10,
  max_iterations = NULL
)
}
\arguments{
\item{...}{Reserved for future extensions; must be empty. Tuning parameters
must be passed by name.}

\item{base_weights}{A numeric vector of base weights, one per observation, or
\code{NULL} for uniform base weights. The estimated weights minimize
\code{sum(w * log(w / base_weights))}.}

\item{distribution_moments}{For continuous exposures, the number of exposure
and covariate marginal moments held equal to the unweighted sample, or
\code{NULL} for the constraint moments. Raised automatically when smaller than
the constraint moments.}

\item{convergence_tolerance}{The solver convergence tolerance on the gradient.}

\item{max_iterations}{The maximum solver iterations, or \code{NULL} for the core
default.}
}
\value{
An \code{bal_entropy} specification, a \link{balance_method}.
}
\description{
\code{bal_entropy()} specifies entropy balancing for \code{\link[=balance]{balance()}}. The weights
minimize the Kullback-Leibler divergence from a set of base weights subject to
the covariate constraints, so among all reweightings that achieve balance the
solution stays as close as possible to the base weights. Entropy balancing
supports binary, categorical, and continuous exposures.
}
\details{
For a binary exposure the average treatment effect reweights each exposure
group to the pooled covariate means, and the average treatment effect on the
treated reweights the control group to the treated covariate means while the
treated group keeps its base weights. When every requested tolerance is zero
the constraints hold exactly and the weights solve smooth estimating
equations, which \code{\link[=balance]{balance()}} records for later linearized inference. A
positive \code{tolerance} in \code{\link[=balance_terms]{balance_terms()}} selects the inexact problem, which
balances each constraint to within the tolerance and does not produce
estimating equations.
}
\examples{
n <- 200
x1 <- rnorm(n)
x2 <- rnorm(n)
df <- data.frame(
  exposure = rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2)),
  x1 = x1,
  x2 = x2
)
fit <- balance(df, exposure, c(x1, x2), method = bal_entropy())
fit

}
\references{
Hainmueller, J. (2012). Entropy balancing for causal effects: A multivariate
reweighting method to produce balanced samples in observational studies.
\emph{Political Analysis}, 20(1), 25-46.
}
