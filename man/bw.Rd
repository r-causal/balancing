% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{bw}
\alias{bw}
\alias{new_bw}
\alias{as_bw}
\alias{is_bw}
\title{Balancing weight vectors}
\usage{
new_bw(x = double(), estimand = NULL, ...)

bw(x = double(), estimand = NULL)

as_bw(x, estimand = NULL)

is_bw(x)
}
\arguments{
\item{x}{A numeric vector of weights for \code{bw()} and \code{new_bw()}, or an object
to test or coerce for \code{is_bw()} and \code{as_bw()}.}

\item{estimand}{A single string naming the target estimand, or \code{NULL}.}

\item{...}{Additional attributes stored on the object (developer use only).}
}
\value{
\itemize{
\item \code{new_bw()}, \code{bw()}, \code{as_bw()}: a \code{bw} vector.
\item \code{is_bw()}: a single logical value.
}
}
\description{
\code{bw} objects are numeric vectors carrying the estimand a set of balancing
weights targets. They are the weight vector stored on a \link{balancing} result
and returned by its \code{\link[=weights]{weights()}} method.

\code{bw} is a sibling of \link[propensity:psw]{propensity::psw}: both inherit the \code{causal_wts} class,
so \code{\link[causalgenerics:is_causal_wt]{causalgenerics::is_causal_wt()}} and \code{\link[causalgenerics:estimand]{causalgenerics::estimand()}} work on
either.
}
\details{
\subsection{Constructors}{
\itemize{
\item \code{bw()} is the user-facing constructor. It coerces \code{x} to double before
building the object.
\item \code{new_bw()} is the low-level constructor for developers. It assumes \code{x} is
already a double vector.
\item \code{as_bw()} coerces an existing numeric vector to a \code{bw} object.
}
}

\subsection{Queries}{
\itemize{
\item \code{is_bw()} tests whether an object is a \code{bw} vector.
\item \code{\link[causalgenerics:estimand]{causalgenerics::estimand()}} reads the target estimand.
}
}

\subsection{Combining}{

Arithmetic preserves the class and estimand, so normalizing weights keeps the
metadata. Combining \code{bw} vectors with matching estimands preserves the class;
combining vectors with different estimands, or a \code{bw} with a
\link[propensity:psw]{propensity::psw}, warns and falls back to a plain double vector.
}
}
\examples{
w <- bw(c(0.5, 1, 1.5), estimand = "ate")
w
is_bw(w)
estimand(w)

# Arithmetic preserves the class.
w / sum(w)

}
