% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{balancing_estimating_equations}
\alias{balancing_estimating_equations}
\title{The estimating-equations container}
\usage{
balancing_estimating_equations(
  parameters = numeric(0),
  psi = numeric(0),
  jacobian = numeric(0),
  weight_jacobian = numeric(0),
  weights_raw = NULL,
  psi_fn = NULL,
  weights_fn = NULL,
  parts_fn = NULL
)
}
\arguments{
\item{parameters}{The fitted parameters, a numeric vector.}

\item{psi}{The \code{n} by \code{p} estimating functions at the solution.}

\item{jacobian}{The \code{p} by \code{p} analytic Jacobian at the solution.}

\item{weight_jacobian}{The \code{n} by \code{p} weight derivatives.}

\item{weights_raw}{The balancing weights whose derivative is
\code{weight_jacobian}, a length-\code{n} numeric vector. The weight derivatives are
stored at whatever per-group reporting scale a method uses internally, so a
consumer that needs the derivative of the reported weights rescales
\code{weight_jacobian} by the ratio of the reported weights to \code{weights_raw}.}

\item{psi_fn}{An optional function re-evaluating \code{psi} at new parameters.}

\item{weights_fn}{An optional function returning the reported balancing
weights at new parameters, a plain double vector with the sampling weights
excluded. At the fitted parameters it reproduces
\code{as.numeric(weights(fit, include_sampling_weights = FALSE))}. The per-group
reporting scale is fixed at the fit rather than recomputed at each set of
parameters, so the function's derivative is \code{weight_jacobian} rescaled by
the ratio of the reported weights to \code{weights_raw}, which is the weight
coupling a stacked variance needs.}

\item{parts_fn}{An optional function returning both of the above at one set
of parameters, a list with elements \code{weights} and \code{psi} holding exactly
what \code{weights_fn} and \code{psi_fn} return there. A method whose estimating
functions are a transformation of its own weights computes the pair
together for the price of one, and a consumer that needs both at every
parameter vector, as a stacked sandwich does, halves its work by asking for
them together. It is an optimization rather than a contract: a consumer
reads it when it is present and falls back to the two functions when it is
not, so a method supplies it only when the saving is real.}
}
\value{
A \code{balancing_estimating_equations} object.
}
\description{
\code{balancing_estimating_equations} holds the pieces a stacked sandwich variance
needs after balancing: the fitted parameters, the per-unit estimating
functions, the analytic Jacobian, and each unit's weight derivatives. It is
produced by methods whose weights solve smooth estimating equations and is
\code{NULL} otherwise.
}
\keyword{internal}
