% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance.R
\name{balance}
\alias{balance}
\title{Estimate balancing weights}
\usage{
balance(
  .data,
  .exposure,
  .covariates,
  method = bw_entropy(),
  estimand = c("ate", "att", "atc", "ato"),
  ...,
  constraints = NULL,
  exposure_type = c("auto", "binary", "categorical", "continuous"),
  focal_level = NULL,
  sampling_weights = NULL
)
}
\arguments{
\item{.data}{A data frame.}

\item{.exposure}{The exposure column, selected with data-masking. Exactly one
column.}

\item{.covariates}{The covariate columns, selected with tidyselect. At least
one column, with no default.}

\item{method}{A \link{balance_method} specification from one of the method
constructors, such as \code{\link[=bw_entropy]{bw_entropy()}}.}

\item{estimand}{The target estimand: \code{"ate"}, \code{"att"}, \code{"atc"} (stored as
\code{"atu"}), or \code{"ato"}. Defaults to \code{"ate"}.}

\item{...}{Reserved; must be empty.}

\item{constraints}{A \code{\link[=balance_terms]{balance_terms()}} specification, or \code{NULL} for the method
default.}

\item{exposure_type}{One of \code{"auto"} (the default), \code{"binary"},
\code{"categorical"}, or \code{"continuous"}.}

\item{focal_level}{The focal exposure level for \code{"att"} and \code{"atc"}. Inferred
for a binary exposure; required for a categorical exposure.}

\item{sampling_weights}{Sampling weights, given as a bare column name or an
external numeric vector, or \code{NULL}.}
}
\value{
A \link{balancing} object.
}
\description{
\code{balance()} fits a balancing method to a data frame, returning weights that
target covariate balance directly. The exposure and covariates are chosen with
tidyselect, the method is one of the method specifications such as
\code{\link[=bw_entropy]{bw_entropy()}}, and the estimand and constraints control what balance the
weights achieve.
}
\details{
The exposure type is detected automatically and announced through an
informational message, which \code{options(balancing.quiet = TRUE)} suppresses. The
estimand vocabulary matches propensity: \code{"atc"} is accepted as a synonym for
the untreated target and stored as \code{"atu"}. \code{"att"} and \code{"atc"} reweight
toward a focal exposure level, inferred for a binary exposure and required
through \code{focal_level} for a categorical exposure. Continuous exposures permit
only \code{"ate"}.

Constraints default to first-moment balance. Pass a \code{\link[=balance_terms]{balance_terms()}}
specification to balance higher moments, interactions, or quantiles, or to
relax exact balance to a tolerance.

A factor covariate expands to one indicator per level, and those indicators
sum to the constant every balancing method carries. One indicator per factor
is therefore redundant with that constant and is dropped, with an
informational alert naming the term. The dropped level is the last one, and
balancing the levels that remain balances it too. The factor stays in
\verb{@covariates}, and \verb{@balance_table} reports the surviving levels rather than
the full set.
}
\examples{
n <- 200
x1 <- rnorm(n)
x2 <- rnorm(n)
df <- data.frame(
  exposure = rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2)),
  x1 = x1,
  x2 = x2
)
fit <- balance(df, exposure, c(x1, x2), method = bw_entropy())
fit
weights(fit)

}
